name: CD

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      skip_pypi:
        description: 'Skip PyPI publish (do not release to PyPI)'
        required: false
        type: boolean
        default: false
      skip_platforms:
        description: 'Skip platform builds (release to GitHub directly)'
        required: false
        type: boolean
        default: false

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

permissions:
  contents: read

jobs:
  # ===========================================================================
  # Quality Gate
  # ===========================================================================
  quality:
    name: Quality Gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Install dependencies
        run: uv sync --dev

      - name: Check Rust formatting
        run: cargo fmt --check

      - name: Check Python formatting
        run: uv run black --check .

      - name: Run Rust clippy
        run: cargo clippy -- -D warnings -A clippy::too_many_arguments

      - name: Run Python ruff
        run: uv run ruff check .

      - name: Verify version sync
        run: |
          CARGO_VER=$(grep '^version = ' Cargo.toml | head -1 | sed 's/version = "\(.*\)"/\1/')
          PYPROJECT_VER=$(grep '^version = ' pyproject.toml | head -1 | sed 's/version = "\(.*\)"/\1/')
          INIT_VER=$(grep '__version__ = ' python/requestx/__init__.py | sed 's/__version__ = "\(.*\)"/\1/')

          echo "Cargo.toml:     $CARGO_VER"
          echo "pyproject.toml: $PYPROJECT_VER"
          echo "__init__.py:    $INIT_VER"

          if [ "$CARGO_VER" != "$PYPROJECT_VER" ] || [ "$CARGO_VER" != "$INIT_VER" ]; then
            echo "::error::Version mismatch! All versions must be identical."
            exit 1
          fi
          echo "All versions in sync: $CARGO_VER"

  # ===========================================================================
  # Build - Linux (glibc)
  # ===========================================================================
  build-linux:
    name: Build Linux ${{ matrix.target }}
    runs-on: ubuntu-22.04
    needs: quality
    if: ${{ !inputs.skip_platforms }}
    strategy:
      fail-fast: false
      matrix:
        target: [x86_64, x86, aarch64, armv7, s390x, ppc64le]
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          manylinux: auto

      - name: Build free-threaded wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist -i python3.14t
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          manylinux: auto

      - name: Upload wheels
        uses: actions/upload-artifact@v5
        with:
          name: wheels-linux-${{ matrix.target }}
          path: dist

  # ===========================================================================
  # Build - Linux (musl)
  # ===========================================================================
  build-musllinux:
    name: Build musllinux ${{ matrix.target }}
    runs-on: ubuntu-22.04
    needs: quality
    if: ${{ !inputs.skip_platforms }}
    strategy:
      fail-fast: false
      matrix:
        target: [x86_64, x86, aarch64, armv7]
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          manylinux: musllinux_1_2

      - name: Build free-threaded wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist -i python3.14t
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          manylinux: musllinux_1_2

      - name: Upload wheels
        uses: actions/upload-artifact@v5
        with:
          name: wheels-musllinux-${{ matrix.target }}
          path: dist

  # ===========================================================================
  # Build - Windows
  # ===========================================================================
  build-windows:
    name: Build Windows ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    needs: quality
    if: ${{ !inputs.skip_platforms }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: windows-latest
            target: x64
            python_arch: x64
          - runner: windows-latest
            target: x86
            python_arch: x86
          - runner: windows-11-arm
            target: aarch64
            python_arch: arm64
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
          architecture: ${{ matrix.python_arch }}

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}

      - uses: actions/setup-python@v6
        with:
          python-version: '3.14t'
          architecture: ${{ matrix.python_arch }}

      - name: Build free-threaded wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist -i python3.14t
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}

      - name: Upload wheels
        uses: actions/upload-artifact@v5
        with:
          name: wheels-windows-${{ matrix.target }}
          path: dist

  # ===========================================================================
  # Build - macOS
  # ===========================================================================
  build-macos:
    name: Build macOS ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    needs: quality
    if: ${{ !inputs.skip_platforms }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-13
            target: x86_64
          - runner: macos-latest
            target: aarch64
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}

      - name: Build free-threaded wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist -i python3.14t
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}

      - name: Upload wheels
        uses: actions/upload-artifact@v5
        with:
          name: wheels-macos-${{ matrix.target }}
          path: dist

  # ===========================================================================
  # Build - Source Distribution
  # ===========================================================================
  build-sdist:
    name: Build sdist
    runs-on: ubuntu-latest
    needs: quality
    steps:
      - uses: actions/checkout@v6

      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist

      - name: Upload sdist
        uses: actions/upload-artifact@v5
        with:
          name: wheels-sdist
          path: dist

  # ===========================================================================
  # Test Rust (needs build-sdist)
  # ===========================================================================
  test-rust:
    name: Test Rust
    runs-on: ubuntu-latest
    needs: build-sdist
    steps:
      - uses: actions/checkout@v6

      - name: Run Rust tests
        run: cargo test --verbose

  # ===========================================================================
  # Test Python (needs test-rust, chained: build-sdist -> test-rust -> test-python)
  # ===========================================================================
  test-python:
    name: Test Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    needs: [test-rust, build-linux]
    if: |
      always() &&
      needs.test-rust.result == 'success' &&
      (needs.build-linux.result == 'success' || needs.build-linux.result == 'skipped')
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.12', '3.13']
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Install dependencies
        run: uv sync --dev

      # When platforms are built, use pre-built wheel
      - name: Download Linux x86_64 wheel
        if: ${{ needs.build-linux.result == 'success' }}
        uses: actions/download-artifact@v5
        with:
          name: wheels-linux-x86_64
          path: dist

      - name: Install wheel
        if: ${{ needs.build-linux.result == 'success' }}
        run: |
          # Find the wheel matching our Python version
          WHEEL=$(ls dist/*.whl | grep -E "cp3(12|13)" | head -1)
          if [ -z "$WHEEL" ]; then
            WHEEL=$(ls dist/*.whl | head -1)
          fi
          echo "Installing: $WHEEL"
          uv pip install "$WHEEL" --force-reinstall

      # When platforms are skipped, build locally
      - name: Build extension locally
        if: ${{ needs.build-linux.result == 'skipped' }}
        run: uv run maturin develop

      - name: Run Python tests
        run: uv run pytest tests_requestx/ -v

  # ===========================================================================
  # GitHub Release
  # ===========================================================================
  release-github:
    name: GitHub Release
    runs-on: ubuntu-latest
    needs: [build-linux, build-musllinux, build-windows, build-macos, build-sdist, test-python]
    if: |
      always() &&
      needs.build-sdist.result == 'success' &&
      needs.test-python.result == 'success' &&
      (
        inputs.skip_platforms ||
        (
          needs.build-linux.result == 'success' &&
          needs.build-musllinux.result == 'success' &&
          needs.build-windows.result == 'success' &&
          needs.build-macos.result == 'success'
        )
      )
    environment:
      name: PROD
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      - name: Get version from tag
        id: version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF#refs/tags/v}"
          else
            VERSION=$(grep '^version = ' pyproject.toml | head -1 | sed 's/version = "\(.*\)"/\1/')
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Download all artifacts
        uses: actions/download-artifact@v5
        with:
          path: artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release-assets
          find artifacts -name '*.whl' -exec cp {} release-assets/ \; 2>/dev/null || true
          find artifacts -name '*.tar.gz' -exec cp {} release-assets/ \;
          echo "Release assets:"
          ls -la release-assets/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: Release v${{ steps.version.outputs.version }}
          files: release-assets/*
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(steps.version.outputs.version, 'rc') || contains(steps.version.outputs.version, 'beta') || contains(steps.version.outputs.version, 'alpha') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GIT_TOKEN }}

  # ===========================================================================
  # PyPI Release
  # ===========================================================================
  release-pypi:
    name: PyPI Release
    runs-on: ubuntu-latest
    needs: release-github
    if: |
      always() &&
      needs.release-github.result == 'success' &&
      !inputs.skip_pypi
    environment:
      name: PROD
      url: https://pypi.org/p/requestx
    permissions:
      id-token: write
      attestations: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v5
        with:
          path: artifacts

      - name: Prepare upload directory
        run: |
          mkdir -p upload
          find artifacts -name '*.whl' -exec cp {} upload/ \; 2>/dev/null || true
          find artifacts -name '*.tar.gz' -exec cp {} upload/ \;
          echo "Files to upload:"
          ls -la upload/

      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v3
        with:
          subject-path: 'upload/*'

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Publish to PyPI
        run: uv publish upload/*
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
